setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
    docker:
      redis: redis/Dockerfile
      cache: cache/Dockerfile
      web: Dockerfile
    config:
      EMAIL_HOST_USER: new.fighting.for.goal@gmail.com
      EMAIL_HOST_PASSWORD: a2+b2=c2
      REDIS_HOST: redis
      REDIS_PORT: 6379
      MEMCACHED_HOST: cache
      MEMCACHED_PORT: 11211
release:
    image: web
    command:
      - ./deployment_tasks.sh
run:
    web: gunicorn Crowdsourcing.wsgi:application